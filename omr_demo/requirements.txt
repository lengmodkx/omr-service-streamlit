streamlit==1.39.1
# opencv-python-headless — moved to bottom to override ultralytics' dep on opencv-python
numpy>=1.24
pandas>=2.0
pyzbar>=0.1.9
Pillow>=10.0,<11
openpyxl>=3.1
streamlit-drawable-canvas>=0.9
# YOLO 模块（可选安装，用于YOLO识别）
ultralytics>=8.2.0
torch>=2.0.0
torchvision>=0.15.0
# 必须在 ultralytics 之后安装，以覆盖其依赖的 opencv-python（非headless版需要libGL）
opencv-python-headless>=4.8
